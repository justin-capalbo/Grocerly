<li class="list-item">
  <span class="item-name"><%= item.name %></span>
  <% unless item.notes.empty? %>
  <span class="item-notes item-info"><%= item.notes %></span>
  <% end %>
  <%= link_to image_tag("trash.png", size: 16), item, class: 'btn-delete', method: :delete, remote: true %>
</li>
